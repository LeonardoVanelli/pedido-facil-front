import styled from "styled-components"

export const Container = styled.div`
  display: flex;
  flex-direction: row;
  border: 1px solid var(--gray-3);
  margin-top: 20px;

  padding: 0 20px;

  width: 100%;

  > form {
    width: 100%;
  }
`
